{
    // In json5 we can have comments!
    "files": [
        // We can omit the contract name since it is the same as the file name
        "src/NodeOperatorManager.sol"
    ],
    "packages": [
        "@openzeppelin=lib/openzeppelin-contracts",
        "forge-std=lib/forge-std/src"
    ],
    "solc": "solc8.24",  // Or use solc-select
    "rule_sanity": "basic",  // Important!
    "verify": "NodeOperatorManager:certora/specs/lesson1/NodeOperatorManager.spec",
    "msg": "Verify fetchNextKeyIndex integrity"
}
